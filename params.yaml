# Input / Output absolute paths, no trailing slash "/"
input:  "/full/path/to/fastqs"
output: "/full/path/to/output"

# Optional overrides (defaults set in nextflow.config):
# primer_scheme: "ARTIC-V5.3.2"        # one of ARTIC-V1/V2/V3/V4/V4.1/V5.3.2 (assets/primers)
# sotc:          "S:L452R,S:E484K"     # comma-separated spike mutations to screen
